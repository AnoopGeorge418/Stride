[project]
name = "backend"
version = "0.1.0"
description = "A strong and structured backend for Stride mobile app."
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "fastapi[standard]>=0.135.3",
    "uvicorn>=0.43.0",
]

[dependency-groups]
dev = [
    "black>=26.3.1",
]
